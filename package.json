{
  "name": "fab-spec",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "scripts": {
    "link-all": "lerna exec -- npm link",
    "build:watch": "lerna run build:watch --stream",
    "build": "lerna run prepack",
    "test": "lerna run test"
  }
}
