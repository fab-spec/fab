{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "scripts": {
    "link-all": "lerna exec -- npm run link",
    "build": "lerna run prepack --stream"
  }
}
