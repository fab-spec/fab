import { ProtoFab, PluginArgs, PluginMetadata } from '@fab/core'

export async function build(args: PluginArgs, proto_fab: ProtoFab) {
  const
  for (const [filename, contents] of proto_fab.files.entries()) {
  }
}
