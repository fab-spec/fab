import { INSPECT_MAX_BYTES, kMaxLength, Buffer, SlowBuffer, isBuffer } from 'buffer-es6'
export { INSPECT_MAX_BYTES, kMaxLength, Buffer, SlowBuffer, isBuffer }

export default {
  INSPECT_MAX_BYTES,
  kMaxLength,
  Buffer,
  SlowBuffer,
  isBuffer,
}
